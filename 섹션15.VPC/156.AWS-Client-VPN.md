# 🌐 AWS 클라이언트 VPN 완벽 정리

**AWS 클라이언트 VPN**은 사용자가 어디서든 OpenVPN 기반의 소프트웨어를 사용하여 AWS 리소스 및 온프레미스 네트워크에 안전하게 접속할 수 있게 해주는 관리형 클라이언트 기반 VPN 서비스입니다.

## 1. 기본 작동 원리

* **연결 방식:** 사용자의 컴퓨터에 **OpenVPN** 클라이언트를 설치한 후, 공용 인터넷을 통해 AWS의 **클라이언트 VPN 엔드포인트**로 접속합니다.
* **주요 이점:** 연결이 설정되면 사용자는 **사설 IP(Private IP)**를 사용하여 VPC 내의 EC2 인스턴스 등에 직접 접근할 수 있습니다.

---

## 2. 주요 네트워크 아키텍처 시나리오

클라이언트 VPN은 단순히 하나의 VPC에 접속하는 것을 넘어, AWS의 다양한 네트워크 서비스와 결합하여 확장할 수 있습니다.

### ① 온프레미스(데이터 센터) 연결

* **구성:** VPC와 온프레미스 간에 **Site-to-Site VPN** 또는 **Direct Connect(DX)**가 연결되어 있는 경우입니다.
* **결과:** 사용자는 클라이언트 VPN으로 VPC에 접속한 후, 해당 연결을 타고 회사 데이터 센터 내의 리소스까지 접근할 수 있습니다. (Traffic: 사용자 → AWS → 온프레미스)

### ② VPC 피어링(Peering) 연동

* **구성:** 클라이언트 VPN이 연결된 VPC A가 다른 VPC B와 피어링되어 있는 경우입니다.
* **결과:** 사용자는 VPC A의 엔드포인트를 통해 접속하여 **VPC B에 있는 리소스**까지 접근이 가능합니다.

### ③ 인터넷 접속 (Internet Access)

* **구성:** 클라이언트 VPN 엔드포인트를 공인 서브넷(Public Subnet)에 연결하고 인터넷 게이트웨이(IGW)를 설정합니다.
* **결과:** 사용자는 VPN 연결 하나로 VPC 내부 리소스는 물론, **VPC를 거쳐 인터넷**까지 안전하게 이용할 수 있습니다.
* **참고:** 프라이빗 서브넷 환경이라면 **NAT 게이트웨이**를 통해 동일한 기능을 구현할 수 있습니다.

### ④ 트랜짓 게이트웨이(Transit Gateway) 활용

* **구성:** 여러 VPC와 온프레미스가 Transit Gateway(TGW)로 중앙 집중화된 경우입니다.
* **결과:** 클라이언트 VPN을 TGW와 연결된 VPC에 붙이면, TGW를 거쳐 연결된 모든 VPC(B, C, D 등)와 온프레미스 네트워크 전체에 액세스할 수 있는 허브 역할을 수행합니다.

---

## 💡 요약 및 핵심 포인트

| 구분 | 내용 |
| --- | --- |
| **프로토콜** | OpenVPN 기반 |
| **접속 경로** | 공용 인터넷 → 클라이언트 VPN 엔드포인트 → AWS 내부망 |
| **확장성** | VPC 피어링, Direct Connect, Site-to-Site VPN, Transit Gateway와 모두 호환 |
| **장점** | 단일 엔드포인트 접속으로 AWS 전체 네트워크 및 인터넷까지 제어 가능 |

> **핵심 결론:** 클라이언트 VPN은 AWS의 모든 네트워크 아키텍처와 통합됩니다. 일단 AWS 내부망으로 안전하게 들어오기만 하면, 기존에 설정된 라우팅을 따라 모든 리소스에 비공개로 접근할 수 있습니다.

## 1. AWS PrivateLink란?

PrivateLink는 수천 개의 VPC에 서비스를 노출할 수 있는 **가장 안전하고 확장 가능한 방법**입니다. 서비스 공급자의 VPC에 있는 애플리케이션을 소비자(고객) VPC에 비공개로 연결합니다.

### 핵심 특징

* **보안성:** 인터넷 게이트웨이(IGW), NAT 게이트웨이, NAT 인스턴스, 라우트 테이블 설정 없이도 비공개 연결이 가능합니다.
* **비노출성:** VPC 피어링과 달리 전체 네트워크를 연결하지 않고, 특정 서비스만 핀포인트로 노출합니다.
* **확장성:** 수천 개의 다른 계정이나 VPC로 서비스를 확장할 수 있습니다.

---

## 2. PrivateLink 작동 원리 및 구성

서비스를 공유하기 위해 다음과 같은 구성 요소가 필요합니다.

1. **서비스 공급자 측 (Service VPC):** * 애플리케이션 앞에 **네트워크 로드 밸런서(NLB)**를 생성합니다.
2. **서비스 소비자 측 (Customer VPC):**
* **탄력적 네트워크 인터페이스(ENI)**를 생성합니다. (이를 인터페이스 VPC 엔드포인트라고 부릅니다.)


3. **트래픽 흐름:**
* 고객 VPC의 애플리케이션 → 소비자 측 ENI → (PrivateLink) → 공급자 측 NLB → 서비스 애플리케이션.



> **💡 중요 포인트:** > 고가용성(High Availability)을 위해 NLB를 여러 가용 영역(AZ)에 배치했다면, 소비자 측의 ENI도 동일한 여러 AZ에 생성해야 결함 내성을 갖출 수 있습니다.

---

## 3. PrivateLink vs VPC 피어링 (Peering)

시험에서 두 방식을 비교하는 문제가 자주 출제됩니다.

| 비교 항목 | VPC 피어링 (Peering) | AWS PrivateLink |
| --- | --- | --- |
| **연결 범위** | 두 VPC의 전체 네트워크가 대화 가능 | 특정 서비스(NLB)만 노출 |
| **복잡성** | 라우트 테이블 관리 및 CIDR 중복 불가 | 라우트 테이블 수정 불필요, IP 중복 무관 |
| **보안** | 네트워크 전체가 노출될 위험이 있음 | 최소한의 서비스만 노출되어 매우 안전함 |

---

## 4. 실전 아키텍처 사례

### A. Direct Connect(DX)를 통한 S3 비공개 접속

온프레미스 데이터 센터에서 Direct Connect를 통해 S3에 접속하는 시나리오입니다.

* **문제 상황:** S3 **게이트웨이 엔드포인트**는 VPC 내부에서만 작동하며, DX를 통한 온프레미스 연동은 지원하지 않습니다.
* **해결책:** 1.  S3용 **인터페이스 VPC 엔드포인트(PrivateLink)**를 생성합니다.
2.  Direct Connect에 **사설 VIF(Private VIF)**를 설정하여 VPC에 연결합니다.
3.  이를 통해 온프레미스 트래픽이 VPC를 거쳐 S3로 비공개 전송됩니다.

### B. VPC 피어링을 통한 타 리전 S3 접속

* **상황:** `us-east-1`의 EC2가 `eu-west-1`의 S3에 비공개로 접속하고 싶을 때.
* **구성 방법:** 1.  S3 버킷이 있는 리전(`eu-west-1`)에 **인터페이스 VPC 엔드포인트**를 생성합니다.
2.  두 리전의 VPC를 **피어링**으로 연결합니다.
3.  `us-east-1`의 EC2는 피어링된 경로를 통해 `eu-west-1`에 있는 인터페이스 엔드포인트 URL로 접근합니다.

---

**추가 학습 팁:**
PrivateLink는 **"네트워크 로드 밸런서(NLB)"**와 **"인터페이스 엔드포인트(ENI)"**의 조합이라는 점을 반드시 기억하세요!
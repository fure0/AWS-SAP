# 🔐 CloudFront 서명된 URL vs S3 미리 서명된 URL 차이

| 구분      | **S3 Pre-signed URL** | **CloudFront Signed URL** |
| ------- | --------------------- | ------------------------- |
| 주체      | Amazon S3             | Amazon CloudFront         |
| 목적      | S3 객체 직접 접근 허용        | CloudFront를 통해 콘텐츠 접근 제어  |
| 접근 경로   | S3 URL 직접 호출          | CloudFront 도메인 통해 접근      |
| 보안 범위   | 개별 객체 단위              | 경로 단위 / 와일드카드 가능          |
| 성능      | CDN 없음 (리전 직접 접근)     | CDN 캐싱 적용 (엣지에서 제공)       |
| 주 사용 사례 | 임시 파일 다운로드/업로드        | 유료 콘텐츠, 스트리밍, 대용량 배포      |

---

# 1️⃣ S3 미리 서명된 URL (Pre-signed URL)

### ✔ 개념

특정 S3 객체에 **일정 시간 동안만 접근 가능하도록 서명된 URL 생성**

### ✔ 특징

* IAM 권한 기반으로 생성
* 만료 시간 설정 가능
* 업로드용(POST/PUT)도 가능
* S3 버킷이 public일 필요 없음

### ✔ 사용 예

* 사용자 프로필 이미지 업로드
* 10분 동안만 다운로드 허용
* 백엔드 없이 클라이언트가 직접 S3 업로드

👉 즉, **"S3 객체 하나를 잠깐 열어준다"** 느낌

---

# 2️⃣ CloudFront 서명된 URL (Signed URL)

### ✔ 개념

CloudFront 배포를 통해 전달되는 콘텐츠에 대해
**특정 사용자만 접근 가능하게 제어**

### ✔ 특징

* CDN 캐싱 유지
* IP 제한 가능
* 기간 제한 가능
* 여러 파일을 하나의 정책으로 제어 가능
* Signed Cookie도 지원

### ✔ 사용 예

* 유료 강의 영상 스트리밍
* 특정 회원만 접근 가능한 PDF
* OTT, SaaS 다운로드

👉 즉, **"CDN을 유지하면서 접근 통제"**

---

# 🔥 가장 큰 차이 (실무 핵심)

### 📌 S3 Pre-signed URL

> 직접 S3에 접근

```
Client → S3
```

### 📌 CloudFront Signed URL

> CloudFront를 통해 접근 (캐싱 + 보안)

```
Client → CloudFront → S3
```

---

# 💡 언제 무엇을 쓰는 게 맞을까?

### ✅ 단순 임시 업로드/다운로드

→ **S3 Pre-signed URL**

### ✅ 대용량 콘텐츠 배포 + 접근 통제

→ **CloudFront Signed URL**

### ✅ 글로벌 사용자 대상

→ 거의 무조건 CloudFront

---

# 🎯 SAA 시험 포인트

* "Restrict access to content distributed by CloudFront"
  → CloudFront Signed URL

* "Allow temporary access to a private S3 object"
  → S3 Pre-signed URL


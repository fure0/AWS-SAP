## 1. CloudHSM 개요

CloudHSM(Hardware Security Module)은 AWS 클라우드에서 전용 하드웨어 보안 모듈을 사용하여 암호화 키를 직접 관리할 수 있게 해주는 서비스입니다.

* **전용 하드웨어:** AWS는 하드웨어만 프로비저닝하며, 단일 테넌트(Single-tenant) 방식으로 제공됩니다.
* **완전한 통제권:** 사용자가 키 생성, 보안, 유지 관리 및 백업에 대한 **전적인 책임**을 집니다.
* **변조 방지:** FIPS 규정을 준수하며, 물리적/논리적 변조 시도가 감지되면 데이터를 보호하기 위해 키를 파기하거나 경고를 발생시킵니다.
* **비용:** **무료 티어가 없으며**, KMS에 비해 상대적으로 고가입니다.

---

## 2. CloudHSM vs KMS 주요 차이점

| 구분 | KMS (Key Management Service) | CloudHSM |
| --- | --- | --- |
| **관리 주체** | AWS가 소프트웨어 및 하드웨어 관리 | 사용자가 키 및 소프트웨어 관리 (AWS는 HW만 제공) |
| **테넌시** | 멀티 테넌트 (Multi-tenant) | **단일 테넌트 (Single-tenant)** |
| **접근 방식** | API 호출 (IAM 권한 기반) | **CloudHSM 클라이언트 소프트웨어** 사용 |
| **권한 관리** | IAM 및 Key Policy 사용 | **HSM 내부 자체 사용자** 관리 |
| **FIPS 등급** | FIPS 140-2 Level 3 | FIPS 140-2 Level 3 |
| **고가용성** | 서비스 자체에 내장됨 | **사용자가 직접 다중 AZ 클러스터 구성** 필요 |
| **무료 티어** | 제공됨 | **제공되지 않음** |

---

## 3. 핵심 아키텍처 및 책임 공유 모델

### AWS의 책임

* 하드웨어 장비의 물리적 가용성 확보
* 클러스터 생성, 설명, 삭제와 같은 인프라 관리 수준의 작업 지원

### 사용자의 책임

* **키 관리:** 생성(Create), 읽기(Read), 업데이트(Update), 삭제(Delete)
* **사용자 관리:** HSM 내부의 암호화된 연결을 통해 액세스할 수 있는 사용자 권한 관리
* **재해 복구:** AWS는 키를 복구해 줄 수 없으므로, 키 분실 시 사용자가 모든 책임을 집니다.

> **💡 데이터베이스 비유:** AWS가 DB 엔진(하드웨어)은 제공하지만, 그 안의 테이블 데이터와 사용자 계정은 사용자가 관리해야 하는 것과 유사합니다.

---

## 4. 고가용성(HA) 및 확장성 구성

CloudHSM을 안정적으로 사용하려면 **클러스터(Cluster)** 환경을 구축해야 합니다.

* **다중 AZ 배치:** 서비스 중단 방지 및 데이터 내구성을 위해 최소 2개 이상의 가용 영역(AZ)에 HSM 디바이스를 분산 배치합니다.
* **클라이언트 연결:** CloudHSM 클라이언트는 클러스터 내의 여러 디바이스와 암호화된 연결을 유지하며 작업을 수행합니다.

---

## 5. 주요 활용 사례

* **S3 SSE-C 암호화:** 사용자가 직접 키를 제공하는 서버 측 암호화(SSE-C) 시, 키 생성 및 보관 용도.
* **SSL/TLS 오프로딩:** 웹 서버의 부하를 줄이기 위해 암호화 가속 기능을 활용.
* **데이터베이스 암호화:** Oracle TDE(Transparent Data Encryption) 등과 통합하여 키 관리.
* **강력한 보안 규정 준수:** 다단계 인증(MFA) 및 해싱(Hashing), 디지털 서명이 필요한 환경.

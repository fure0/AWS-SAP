## 1. Amazon Redshift 개요

Redshift는 PostgreSQL 기반의 **OLAP(온라인 분석 처리)**용 데이터 웨어하우스 서비스입니다.

* **OLTP vs OLAP:**
  * **OLTP (RDS):** 트랜잭션 처리 중심.
  * **OLAP (Redshift):** 복잡한 쿼리 분석 및 데이터 웨어하우징 중심.


* **스토리지 방식:** **열 기반 저장(Columnar Storage)** 방식을 사용합니다. 특정 열의 합계나 평균을 계산할 때 전체 행을 읽지 않아도 되므로 분석 성능이 비약적으로 향상됩니다.
* **성능 엔진:** **MPP(Massively Parallel Processing)** 아키텍처를 통해 쿼리를 클러스터 내 여러 노드에 분산 실행하여 페타바이트급 데이터를 빠르게 처리합니다.

---

## 2. 클러스터 아키텍처 및 구성

### 노드 유형

* **리더 노드(Leader Node):** 쿼리 접수, 실행 계획 최적화, 컴퓨트 노드 결과 집계 수행.
* **컴퓨트 노드(Compute Node):** 실제 데이터를 저장하고 쿼리를 실행. 각 노드는 최대 16TB 공간을 가질 수 있으며 100개 이상의 노드로 확장 가능합니다.

### 실행 모드

* **프로비저닝 클러스터:** 온디맨드 서버를 직접 구성.
* **서버리스(Serverless):** 인프라 관리 없이 SQL 인터페이스로 바로 쿼리 수행.

---

## 3. 데이터 로딩 및 통합

* **데이터 로드 방법:**
  * **S3:** `COPY` 명령어를 사용하여 대량 로드 (가장 권장됨).
  * **Kinesis Data Firehose:** 거의 실시간 데이터 로드.
  * **기타:** DynamoDB 통합, DMS(Database Migration Service) 활용.


* **시각화 도구 연동:** Amazon QuickSight, Tableau 등과 통합하여 대시보드 생성.

---

## 4. 백업 및 재해 복구 (DR)

시험에서 매우 중요한 비중을 차지하는 부분입니다.

* **스냅샷:** S3에 증분 방식으로 저장되는 시점 백업입니다.
* **자동:** 8시간마다 또는 5GB 변경 시 발생 (보존 기간 설정 가능).
* **수동:** 사용자가 직접 생성하며, 삭제 전까지 영구 보존.


* **교차 리전 스냅샷 복사(Cross-Region Snapshot Copy):** 재해 복구 전략에 필수적입니다.
* **KMS 암호화 시 주의사항:** 소스 리전의 스냅샷을 대상 리전의 KMS 키로 복사하려면 **'스냅샷 복사 권한(Snapshot Copy Grant)'**을 생성하고 유지해야 합니다.



---

## 5. 주요 확장 및 최적화 기능

### Redshift Spectrum

데이터를 Redshift로 로드하지 않고 **S3에 있는 데이터를 직접 쿼리**하는 기능입니다. 수천 개의 스펙트럼 노드를 동적으로 할당하여 대규모 쿼리를 처리합니다.

### WLM (Workload Management)

쿼리의 우선순위를 관리하여 리소스 경합을 방지합니다.

* **사용 사례:** 오래 걸리는 쿼리(Long-running)가 짧고 빠른 쿼리(Short-running)를 중단시키지 않도록 전용 큐(Queue)를 할당.
* **자동 WLM:** Redshift가 큐와 자원을 자동 관리.
* **수동 WLM:** 사용자가 직접 큐를 정의하고 런타임에 쿼리를 라우팅.

### 동시성 확장 (Concurrency Scaling)

사용자나 쿼리 요청이 급증할 때 **자동으로 클러스터 용량을 추가**하여 성능을 유지합니다. 초 단위로 과금되며, WLM 설정에 따라 특정 큐의 쿼리만 이 기능을 사용하게 할 수도 있습니다.

---

## 6. 보안 및 네트워크

* **VPC 보안:** 클러스터는 VPC 내에서 실행됩니다. IAM(접근 제어), KMS(데이터 암호화), CloudWatch(모니터링)를 지원합니다.
* **향상된 VPC 라우팅(Enhanced VPC Routing):** `COPY` 및 `UNLOAD` 작업 시 트래픽이 공인 인터넷을 타지 않고 VPC 내부를 통해 흐르도록 강제하여 보안과 성능을 높이고 비용을 절감합니다.

> **💡 핵심 팁:** 쿼리가 지속적으로 발생하는 환경은 **Redshift**가 유리하지만, 쿼리가 가끔씩 발생하는 산발적인 환경이라면 서버리스 쿼리 서비스인 **Amazon Athena**가 더 경제적일 수 있습니다.
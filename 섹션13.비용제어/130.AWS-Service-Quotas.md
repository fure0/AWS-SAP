## ☁️ AWS Service Quotas 핵심 정리

AWS Service Quotas는 AWS 리소스 사용량이 할당량(Limit) 임계값에 도달하기 전에 미리 파악하고 관리할 수 있게 해주는 서비스입니다.

### 1. 주요 기능 및 목적

* **안전 장치(Safety Mechanism):** 사용자가 실수로 AWS 서비스를 남용하거나 과도한 비용이 발생하는 것을 방지합니다.
* **스로틀링(Throttling) 방지:** 할당량 최대치에 도달하여 서비스가 중단되거나 성능이 저하되는 상황을 미연에 방지합니다.
* **중앙 집중식 관리:** 여러 AWS 서비스의 할당량을 한곳에서 확인하고 증설 요청을 보낼 수 있습니다.

### 2. CloudWatch 연동 및 알람 구조

Service Quotas 콘솔 내에서 직접 **CloudWatch 경보(Alarm)**를 생성하여 모니터링할 수 있습니다.

* **작동 프로세스:**
  1. **임계값 설정:** 특정 서비스(예: Lambda 동시성 한도)의 사용량 임계값을 설정합니다.
  2. **모니터링:** Service Quotas가 해당 리소스의 사용량을 지속적으로 감시합니다.
  3. **알림 발송:** 사용량이 설정한 임계값(예: 80%~90%)에 도달하면 CloudWatch 경보를 통해 사용자에게 알림을 보냅니다.



### 3. 실제 활용 예시: Lambda 동시 실행(Concurrency)

| 단계 | 설명 |
| --- | --- |
| **상황 설정** | Lambda의 동시성 한도가 10,000으로 설정되어 있음 |
| **경보 생성** | 임계값을 9,000으로 설정하고 CloudWatch 경보 연결 |
| **알림 발생** | 트래픽 증가로 동시 실행 수가 9,000에 육박하면 알림 수신 |
| **대응 조치** | **할당량 증설 요청**을 보내거나, 불필요한 **리소스를 종료**하여 여유분 확보 |

---

> **💡 핵심 요약**
> Service Quotas는 단순히 한도를 확인하는 것을 넘어, **CloudWatch와 연동해 선제적으로 리소스를 관리**할 수 있게 해주는 도구입니다. 시험이나 실무에서는 "한도 도달 전 알림"이 필요할 때 가장 먼저 고려해야 할 서비스입니다.
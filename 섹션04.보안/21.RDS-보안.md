## 🔐 AWS RDS 보안 핵심 요약

### 1. 데이터 암호화 (Encryption)

데이터를 보호하는 방식은 크게 '저장 시'와 '전송 시'로 나뉩니다.

* **저장 데이터 암호화 (At-Rest):**
* **KMS 활용:** 기본 EBS 볼륨 및 스냅샷에 대해 AWS KMS 암호화 키를 사용하여 데이터를 암호화할 수 있습니다.
* **TDE (Transparent Data Encryption):** **Oracle** 및 **SQL Server**에서 제공하는 투명한 데이터 암호화 옵션을 지원합니다.


* **전송 데이터 암호화 (In-Transit):**
* **SSL/TLS:** 모든 DB 엔진에 대해 전송 중인 데이터를 SSL 암호화로 보호할 수 있습니다.



### 2. 인증 및 권한 관리 (Authentication & Authorization)

* **IAM 인증 (IAM Database Authentication):**
* **지원 엔진:** **MySQL**과 **PostgreSQL**에서만 가능합니다. (시험 단골 함정!)
* **주의 사항:** IAM은 '인증(로그인)' 단계까지만 담당합니다. 실제 DB 내부의 테이블 접근 권한(Select, Update 등) 부여는 **RDS 내부**에서 관리해야 합니다.
* **장점:** 별도의 DB 비밀번호를 관리할 필요 없이 IAM 사용자로 중앙 집중식 관리가 가능합니다.



### 3. 스냅샷 보안 및 복사

* **암호화 전환:** 암호화되지 않은 기존 RDS 스냅샷을 암호화된 스냅샷으로 복사하여 보안 수준을 높일 수 있습니다. 이는 데브옵스(DevOps) 환경에서 보안 규정을 준수할 때 자주 사용되는 방식입니다.

### 4. 로깅 및 모니터링 (Logging)

* **CloudTrail의 한계:** CloudTrail은 RDS API 호출(인스턴스 생성, 삭제 등)은 기록하지만, **DB 내부에서 발생하는 SQL 쿼리(Query)는 추적할 수 없습니다.**
* **쿼리 로깅:** 쿼리 수준의 상세 로그가 필요하다면 각 DB 엔진의 Native 로그(General Log, Slow Query Log)나 특정 데이터베이스 모니터링 도구를 별도로 설정해야 합니다.

---

## ⚠️ 시험 대비 체크포인트 (함정 주의)

시험 문제에서는 다음과 같은 오답 조합을 제시할 확률이 높습니다.

| 보안 기능 | 지원 대상 (정답) | 오답 함정 예시 |
| --- | --- | --- |
| **TDE** | Oracle, SQL Server | MySQL과 연결하는 경우 |
| **IAM 인증** | MySQL, PostgreSQL | Oracle, SQL Server와 연결하는 경우 |
| **로그 추적** | 특정 모니터링 도구/DB 로그 | 쿼리 추적에 CloudTrail을 쓴다는 경우 |

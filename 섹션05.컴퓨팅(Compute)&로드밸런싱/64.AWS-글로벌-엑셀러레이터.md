# 🌐 AWS Global Accelerator 완벽 정리

**AWS Global Accelerator**는 AWS의 전용 내부 네트워크를 활용하여 사용자 트래픽의 성능을 최적화하고 가용성을 높여주는 네트워크 서비스입니다.

---

## 1. 핵심 동작 원리 및 특징

### 📍 Anycast IP 사용

* **고정 IP 제공:** Global Accelerator를 생성하면 전 세계 어디서든 접속 가능한 **2개의 고정 Anycast IP**가 할당됩니다.
* **Edge Location 라우팅:** 사용자의 트래픽은 가장 가까운 **Edge Location**으로 먼저 유입됩니다.
* **내부 네트워크 활용:** Edge Location에 들어온 트래픽은 공용 인터넷이 아닌 **AWS 전용 프라이빗 네트워크**를 타고 목적지(ALB, EC2 등)까지 이동합니다. 이 과정에서 지연 시간(Latency)이 획기적으로 줄어듭니다.

### 🎯 지원 엔드포인트 및 클라이언트 IP 보존

* **대상(Target):** Elastic IP, EC2 인스턴스, ALB(Application Load Balancer), NLB(Network Load Balancer).
* **네트워크 종류:** 공용(Public) 및 사설(Private) 엔드포인트 모두 지원.
* **IP 보존:** Elastic IP 엔드포인트를 제외하고, **클라이언트의 원래 IP를 엔드포인트까지 전달(Preservation)**할 수 있습니다.

---

## 2. 주요 장점

| 구분 | 주요 내용 |
| --- | --- |
| **일관된 성능** | 지연 시간이 가장 낮은 경로로 지능형 라우팅을 수행합니다. |
| **캐시 문제 해결** | IP가 고정되어 있으므로, DNS 변경 시 발생하는 클라이언트 캐시 문제가 없습니다. |
| **빠른 장애 조치** | 상태 검사(Health Check)를 통해 문제가 발생하면 **1분 이내**에 정상 지역으로 트래픽을 돌립니다. |
| **보안 강화** | 두 개의 IP만 방화벽(Whitelist)에 추가하면 되며, **AWS Shield**와 통합되어 DDoS 공격을 방어합니다. |

---

## 3. CloudFront vs Global Accelerator 비교

두 서비스 모두 AWS의 글로벌 네트워크와 Edge Location을 사용하지만, 목적이 다릅니다.

| 특징 | CloudFront | Global Accelerator |
| --- | --- | --- |
| **주요 목적** | 캐싱을 통한 콘텐츠 전송 가속화 | 네트워크 경로 최적화를 통한 성능 향상 |
| **콘텐츠 유형** | 이미지, 비디오(정적), API(동적) | TCP/UDP 패킷 자체를 프록시 전송 |
| **지원 프로토콜** | HTTP / HTTPS (L7) | **TCP / UDP (L4)** 및 HTTP |
| **사용 사례** | 웹사이트, 비디오 스트리밍 | 게임(UDP), IoT(MQTT), VoIP, 고정 IP가 필요한 경우 |

---

## 4. 요약: 언제 사용하면 좋을까요?

1. **UDP 활용 서비스:** 게임, VoIP(인터넷 전화)처럼 지연 시간에 민감한 서비스.
2. **IoT 서비스:** MQTT 프로토콜을 사용하는 사물인터넷 장치 연결.
3. **빠른 지역 장애 조치:** 재해 복구(DR) 상황에서 결정론적이고 신속한 전환이 필요할 때.
4. **보안 요구 사항:** 클라이언트 측에서 특정 고정 IP만 허용(IP Whitelisting)해야 할 때.

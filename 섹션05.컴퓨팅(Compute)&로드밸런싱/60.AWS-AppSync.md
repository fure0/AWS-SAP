## 1. AWS AppSync 개요

AWS AppSync는 백엔드에서 **GraphQL**을 사용하는 완벽한 관리형 서비스입니다. 여러 데이터 소스의 데이터를 통합하여 클라이언트에게 효율적으로 전달하는 역할을 합니다.

* **GraphQL 기반:** 클라이언트가 필요한 데이터만 정확히 요청하고 받을 수 있습니다.
* **데이터 통합:** 단일 또는 다수의 데이터 소스(NoSQL, RDBMS, HTTP API 등)로부터 데이터를 종합합니다.
* **실시간 및 오프라인 기능:**
  * **WebSocket/MQTT**를 활용한 실시간 데이터 업데이트 지원.
  * 모바일 앱의 **로컬 데이터 액세스 및 오프라인 동기화** 가능.



---

## 2. AppSync의 작동 원리 및 데이터 소스

AppSync는 클라이언트와 백엔드 데이터 사이에서 중계자 역할을 하며, **리졸버(Resolver)**를 통해 데이터를 처리합니다.

### **주요 구성 요소**

1. **GraphQL 스키마:** 데이터 구조를 정의합니다.
2. **리졸버(Resolver):** 스키마의 각 필드에 대해 데이터를 어떻게 가져올지 정의하는 로직입니다.
3. **데이터 소스:**
   * **DynamoDB:** NoSQL 데이터 저장소.
   * **Amazon Aurora:** 관계형 데이터베이스.
   * **OpenSearch (Elasticsearch):** 검색 및 분석 엔진.
   * **AWS Lambda:** 사용자 지정 로직을 통한 모든 소스 연결.
   * **HTTP:** 외부 공용 HTTP API 엔드포인트.



### **모니터링**

* **CloudWatch 메트릭 및 로그**를 통해 AppSync의 성능과 오류를 실시간으로 모니터링합니다.

---

## 3. AppSync와 Amazon Cognito 통합 (보안)

AppSync는 Cognito 사용자 풀과 통합하여 **그룹 기반의 세밀한 권한 제어**를 수행할 수 있습니다.

### **인증 프로세스**

1. **로그인:** 클라이언트가 Amazon Cognito를 통해 인증을 수행합니다.
2. **토큰 발급:** Cognito는 클라이언트에게 **JWT(JSON Web Token)**를 발급합니다.
3. **요청:** 클라이언트는 JWT 토큰을 포함하여 AppSync에 요청을 보냅니다.
4. **검증:** AppSync는 토큰을 확인하고, 사용자가 속한 **Cognito 그룹**에 따라 액세스 권한을 판단합니다.

### **스키마 기반 권한 설정 예시**

`@aws_auth` 지시어를 사용하여 특정 그룹만 특정 작업(Mutation, Query)을 수행하도록 제한할 수 있습니다.

* **Query (조회):** `bloggers`, `Readers` 그룹 모두 허용.
* **Mutation (수정/추가):** `bloggers` 그룹만 게시물 추가 가능.

---

## 4. 시험 대비 핵심 포인트 (Cheat Sheet)

> **💡 이런 키워드가 나오면 AppSync가 정답일 확률이 높습니다!**
> * **GraphQL** 기술이 언급될 때.
> * 다양한 데이터 소스에서 데이터를 **하나의 엔드포인트**로 통합해야 할 때.
> * **실시간(Real-time)** 데이터 대시보드나 알림 기능이 필요할 때 (WebSocket).
> * 모바일 앱의 **오프라인 데이터 동기화**가 요구될 때.
> * Cognito 그룹별로 API 접근 권한을 **세밀하게 제어**해야 할 때.
> 
> 
